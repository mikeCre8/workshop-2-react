import ReactDOM from 'react-dom/client';
import "bootstrap/dist/css/bootstrap.css";

const greeting = <h1>Welcome</h1>;

ReactDOM.createRoot(document.getElementById("root")).render(greeting) // all required node_modules are installed. Ready to commit & push. (Step 11)